<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
      <%= f.label :song_image %><br />
      <%= f.file_field :song_image %>
  </div>
  <div class="field">
  <%= f.text_field :remote_song_image_url, placeholder: "Web URL for Song Cover" %>
  </div>
  <div class="field">
  <%= f.label :song_upload %><br />
  <%= f.file_field :song_upload %>
  </div>
  <div class="field">
  <%= f.text_field :remote_song_upload_url, placeholder: "Web URL for Song Upload" %>
  </div>
  <div class="field">
    <%#= f.label :rating %><br>
    <%#= f.number_field :rating %>
  </div>
  <div class="field">
     <%= f.collection_select(:genre_id, Genre.all, :id, :name) %><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
