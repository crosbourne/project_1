<%= form_for(@genre) do |f| %>
  <% if @genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genre.errors.count, "error") %> prohibited this genre from being saved:</h2>

      <ul>
      <% @genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
  <%= f.label :genre_image %><br />
  <%= f.file_field :genre_image %>
  </div>
  <div class="field">
      <%= f.text_field :remote_genre_image_url, placeholder: "Web URL for Genre Cover" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

